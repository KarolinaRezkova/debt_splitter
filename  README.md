# Debt Splitter

A lightweight backend utility to track shared group expenses and calculate balances.

## Installation
```bash
pip install -r requirements.txt
```

## Usage
Create a `Group`, add members, log `Expense` records, and use `DebtCalculator` to find the final balances.
*TODO: Support custom split weights (e.g., when one person pays 60% and another 40%).*
